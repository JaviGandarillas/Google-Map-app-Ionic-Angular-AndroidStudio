import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.javi.prueba.instacrops',
  appName: 'prueba-instacrops',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
